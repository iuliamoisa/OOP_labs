#pragma once

class GlobalFunctions {
	//namespace SimpleCompare {
	int CompName(Student& a, Student& b);
	int CompGradeM(Student& a, Student& b);
	int CompGradeE(Student& a, Student& b);
	int CompGradeH(Student& a, Student& b);
	//}
	//namespace ComplexCompare {
	int CompAvgGrade(Student& a, Student& b);
	//int ByHighestGrade(const Student& a, const Student& b);
//}
};


/*
#pragma once
#include "student.h"
namespace GlobalFunctions {
	int byName(student a, student b);
	int byMath(student a, student b);
	int byEng(student a, student  b);
	int byHist(student a, student b);
	int byAverage(student a, student b);
}
*/
